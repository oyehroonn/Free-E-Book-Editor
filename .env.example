# App URL (used for metadata and OpenGraph)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# No database or auth setup required — all data is stored in /data/*.json
# All uploaded images are saved to /public/uploads/
